{
	"name": "tensor4",
	"$schema": "../../node_modules/nx/schemas/project-schema.json",
	"sourceRoot": "apps/tensor4/src",
	"projectType": "application",
	"targets": {
		"build": {
			"executor": "@nx/vite:build",
			"outputs": [
				"{options.outputPath}"
			],
			"defaultConfiguration": "production",
			"options": {
				"outputPath": "apps/tensor4/dist"
			},
			"configurations": {
				"development": {
					"mode": "development"
				},
				"production": {
					"mode": "production"
				}
			}
		},
		"dev": {
			"executor": "@nx/vite:dev-server",
			"defaultConfiguration": "development",
			"options": {
				"buildTarget": "tensor4:build"
			},
			"configurations": {
				"development": {
					"buildTarget": "tensor4:build:development",
					"hmr": true
				},
				"production": {
					"buildTarget": "tensor4:build:production",
					"hmr": false
				}
			}
		}
	},
	"tags": []
}